// TODO (Week 2, Day 2+) — Krishna
// Wire up graph.controller.js here once it's built:
//   GET/POST /api/graph, GET/PUT/DELETE /api/graph/:id,
//   POST /api/graph/:id/deploy, POST /api/graph/:id/stop
// Protect all of these with requireAuth from ../middleware/authMiddleware.js
